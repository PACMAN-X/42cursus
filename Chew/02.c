#include "stdio.h"

// STDardInputOutput.h 입니다. header파일을 추가하게 되면 복사하여 현재의 파일에서 사용할 수 있게 됩니다.

int main() { // 반환값은 int(integer type) main함수는 모든 C프로그램이 처음으로 시작하는 부분입니다. 
    
    printf("hello, world");
    return (0);
}